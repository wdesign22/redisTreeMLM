{% extends 'base.html.twig' %}

{% block body %}
    <h1>Post2</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post2.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post2.content }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('post2_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('post2_edit', { 'id': post2.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
